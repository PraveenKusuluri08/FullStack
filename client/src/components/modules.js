import CreateIcon from '@material-ui/icons/Create';

const modules=[
    {
        text:"CreateBlog",
        icon:<CreateIcon/>,
        moduleName:"createBlog",
        link:"/user/createBlog"
    }
]

export default modules